{
  "name": "jobs-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "build:demo": "VITE_DEMO_MODE=true vite build --outDir demo-dist --base /simple-durable-jobs/demo/",
    "font:subset": "echo \"pyftsubset JetBrainsMono-Regular.ttf --output-file=src/assets/fonts/jbmono-subset.woff2 --flavor=woff2 --unicodes=U+0000-00FF,U+2018-201F,U+2026,U+2190-2193,U+25CF,U+2713 --layout-features='tnum,zero,calt,liga'\"",
    "test:unit": "vitest run src",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@tsconfig/svelte": "^5.0.4",
    "@types/node": "^25.3.5",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.6.0",
    "uuid": "^14.0.1",
    "vite": "^5.0.0",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^1.10.0",
    "@connectrpc/connect": "^1.6.1",
    "@connectrpc/connect-web": "^1.6.1"
  },
  "//lockfile": "package-lock.json is the ONE lockfile: CI runs `npm ci`, and the committed go:embed'd dist is gated on matching a fresh build from it. A pnpm-lock.yaml was also committed and pinned a DIFFERENT Svelte compiler (5.56.2 vs 5.51.3), so the two managers produced different bundle bytes and different hashed filenames — whichever one a maintainer used, the other would have tripped that gate. Do not reintroduce a second lockfile."
}
